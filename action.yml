name: '@HyperCubeSphere/release'
author: 'HyperCubeSphere'
description: 'Run HyperCubeSphere/release on a repo'
outputs:
  release-tag:
    description: 'The tag that was released on success'
  release-version:
    description: 'The version that was released on success'
branding:
  icon: 'package'
  color: 'yellow'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/hypercubesphere/release:1.0.0'

